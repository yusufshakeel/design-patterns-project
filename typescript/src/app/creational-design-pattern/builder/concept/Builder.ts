// this the builder interface

interface Builder {
  // eslint-disable-next-line no-unused-vars
  withProp1(prop1: string): void;
  // eslint-disable-next-line no-unused-vars
  withProp2(prop2: string): void;
  // eslint-disable-next-line no-unused-vars
  withProp3(prop3: string): void;
}

export default Builder;